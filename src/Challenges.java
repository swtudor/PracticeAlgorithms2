public class Challenges {

    /**
     * Given an age in years, return the age in number of days
     * Use 365 as the length of the year
     * Ignore leap years
     * return 0 if the user input is a negative
     * @param age
     * @return int
     */
    public static int ageByDays(int age){
        // write your solution below
        // replace the return statement with your own
        return 0;
    }

    /**
     * Given a number of minutes, return the number of seconds
     * return 0 if the user input is negative.
     * @param minutes
     * @return int
     */
    public static int convertToSeconds(int minutes){
        // write your solution below
        // replace the return statement with your own
        return 0;
    }

    /**
     *  Given an unsorted array, return the nth smallest integer in the array
     *  return -1 if the number given is larger than the array
     * @param testArray
     * @param num
     * @return int
     */
    public static int findNthSmallest(int[] testArray, int num){
        // write your solution below
        // replace the return statement with your own
        return 0;
    }

    /**
     * Given a string, return a new String with the words rearranged from shortest to longest
     * If the words are the same length, order them alphabetically
     * @param str
     * @return str
     */
    public static String sortByLength(String str){
        // write your solution below
        // replace the return statement with your own
        return "replace with your answer";
    }

    /**
     * Given a string, return an int of how many vowels are contained in the string
     * a, e, i, o, u count in this challenge, Y is not a vowel
     * @param str
     * @return int
     */
    public static int countVowels(String str){
        // write your solution below
        // replace the return statement with your own
        return 0;
    }

}
